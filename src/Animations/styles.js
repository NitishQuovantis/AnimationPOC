import {StyleSheet} from 'react-native';

const styleSheet = StyleSheet.create({
  containerStyle: {
    flex: 1,
    justifyContent: 'center',
    alignContent: 'center',
    alignItems: 'center',
  },
  boxStyle: {width: 100, height: 100, backgroundColor: 'red'},

  contentStyle: {
    margin: 20,
  },
});

export default styleSheet;
